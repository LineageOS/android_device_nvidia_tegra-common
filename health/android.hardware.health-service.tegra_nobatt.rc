service vendor.health-default /vendor/bin/hw/android.hardware.health-service.tegra_nobatt
    class hal
    user system
    group system
    capabilities WAKE_ALARM BLOCK_SUSPEND
    file /dev/kmsg w
